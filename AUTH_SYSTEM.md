# 🔐 Система авторизации - Документация

## ✅ Добавлена полноценная система входа и регистрации!

**Дата:** 5 ноября 2025  
**Версия:** 4.0  
**Статус:** ✅ ЗАВЕРШЕНО

---

## 🎯 Что было добавлено

### 1. **Экран логина**
- Показывается при первом открытии
- Поля: Логин и Пароль
- Кнопка "Войти"
- Ссылка "Ещё нет профиля? Регистрация"

### 2. **Экран регистрации**
- Переключается по ссылке
- Поля: Логин и Пароль
- Кнопка "Регистрация"
- Ссылка "Уже есть профиль? Войти"

### 3. **Тестовый пользователь**
- Логин: `test`
- Пароль: `test`
- Создается автоматически при первом запуске

### 4. **Сессии**
- После входа сессия сохраняется
- При обновлении страницы не нужно входить заново
- При выходе - возврат на экран логина

### 5. **Безопасность**
- Хранение пользователей в localStorage
- Проверка логина и пароля
- Минимальная длина: 3 символа
- Уникальные логины

---

## 🎮 Как это работает

### **Первый запуск:**

```
1. Открываете index.html
2. Видите экран логина:
   ┌────────────────────────┐
   │ Шахматный тренажер     │
   │                        │
   │ [     Логин      ]    │
   │ [    пароль      ]    │
   │ [     Войти      ]    │
   │                        │
   │ Ещё нет профиля?       │
   │   Регистрация          │
   └────────────────────────┘

3. Вход тестовым пользователем:
   Логин: test
   Пароль: test
   
4. После входа → игра!
```

---

### **Регистрация нового пользователя:**

```
1. На экране логина кликните "Регистрация"
2. Появится экран регистрации:
   ┌────────────────────────┐
   │ Шахматный тренажер     │
   │                        │
   │ [     Логин      ]    │
   │ [    пароль      ]    │
   │ [  Регистрация   ]    │
   │                        │
   │ Уже есть профиль?      │
   │      Войти             │
   └────────────────────────┘

3. Введите новые логин и пароль (минимум 3 символа)
4. Нажмите "Регистрация"
5. Автоматический вход → игра!
```

---

### **Работа в игре:**

```
После успешного входа:
- Экран логина скрывается
- Появляется игровой экран
- В левой панели показано имя пользователя
- Кнопка "Выйти" для выхода из аккаунта
- Форма регистрации внутри игры УБРАНА ✅
```

---

### **Выход из аккаунта:**

```
1. Нажмите "Выйти" в левой панели
2. Игра останавливается
3. Сессия удаляется
4. Возврат на экран логина
```

---

## 🔧 Технические детали

### **Хранение данных**

#### 1. Список пользователей
**Ключ:** `chessTrainerUsers`  
**Формат:** JSON массив
```javascript
[
  {
    username: "test",
    password: "test",
    createdAt: "2025-11-05T12:00:00.000Z"
  },
  {
    username: "alex",
    password: "12345",
    createdAt: "2025-11-05T13:00:00.000Z"
  }
]
```

#### 2. Текущая сессия
**Ключ:** `chessTrainerSession`  
**Формат:** JSON объект
```javascript
{
  username: "test",
  loginTime: "2025-11-05T12:00:00.000Z"
}
```

---

### **Новые функции**

#### 1. `initializeAuth()`
- Проверяет наличие активной сессии
- Если есть → показывает игру
- Если нет → показывает логин

#### 2. `initializeUsers()`
- Создает тестового пользователя (test/test)
- Вызывается автоматически при старте

#### 3. `handleLogin()`
- Проверяет логин и пароль
- Создает сессию при успешном входе
- Показывает игровой экран

#### 4. `handleRegister()`
- Валидация данных (минимум 3 символа)
- Проверка на дубликаты логина
- Автоматический вход после регистрации

#### 5. `showGameScreen()`
- Скрывает экран авторизации
- Показывает игру
- Обновляет имя пользователя

#### 6. `showAuthScreen()`
- Скрывает игру
- Показывает экран логина
- Очищает поля ввода

#### 7. `logoutUser()` (обновлена)
- Удаляет сессию
- Останавливает игру
- Возвращает на экран логина

---

## 🎨 Дизайн экрана авторизации

### **Цвета:**
- Поля ввода: Зеленый (#4CAF50)
- Кнопка: Темно-зеленый (#2E7D32)
- Ссылки: Зеленый (#4CAF50)
- Placeholder: Белый с прозрачностью

### **Размеры:**
- Поля ввода: padding 20px, font-size 1.1rem
- Кнопка: padding 20px, font-size 1.2rem
- Контейнер: max-width 400px

### **Анимации:**
- Hover на кнопке: transform + тень
- Hover на ссылке: подчеркивание
- Focus на поле: свечение

---

## 🧪 Тестирование

### **Тест 1: Вход тестовым пользователем**

1. Откройте `index.html`
2. ✅ Должен показаться экран логина
3. Введите:
   - Логин: `test`
   - Пароль: `test`
4. Нажмите "Войти" (или Enter)
5. ✅ Должно показаться: "Добро пожаловать, test!"
6. ✅ Должна открыться игра

---

### **Тест 2: Регистрация нового пользователя**

1. На экране логина кликните "Регистрация"
2. ✅ Форма должна измениться
3. Введите новые данные:
   - Логин: `player1` (минимум 3 символа)
   - Пароль: `123456` (минимум 3 символа)
4. Нажмите "Регистрация"
5. ✅ Должно показаться: "Регистрация успешна!"
6. ✅ Автоматический вход в игру

---

### **Тест 3: Сохранение сессии**

1. Войдите в игру
2. Обновите страницу (F5)
3. ✅ Должны остаться в игре (не вернуться на логин)
4. ✅ Имя пользователя на месте

---

### **Тест 4: Выход из аккаунта**

1. В игре нажмите "Выйти"
2. ✅ Должно показаться: "Вы вышли из аккаунта"
3. ✅ Должен показаться экран логина
4. ✅ Поля ввода пустые

---

### **Тест 5: Переключение между формами**

1. На экране логина кликните "Регистрация"
2. ✅ Форма логина исчезнет
3. ✅ Форма регистрации появится
4. Кликните "Войти"
5. ✅ Вернется форма логина

---

### **Тест 6: Валидация**

**Пустые поля:**
- Оставьте поля пустыми
- Нажмите "Войти"
- ✅ Должно показаться: "Введите логин и пароль!"

**Короткий логин:**
- Введите логин: `ab` (2 символа)
- Нажмите "Регистрация"
- ✅ Должно показаться: "Логин должен быть минимум 3 символа!"

**Короткий пароль:**
- Введите пароль: `12` (2 символа)
- Нажмите "Регистрация"
- ✅ Должно показаться: "Пароль должен быть минимум 3 символа!"

**Дубликат логина:**
- Попробуйте зарегистрировать пользователя "test"
- ✅ Должно показаться: "Пользователь с таким логином уже существует!"

**Неверный пароль:**
- Введите логин: `test`
- Введите пароль: `wrong`
- ✅ Должно показаться: "Неверный логин или пароль!"

---

## 📊 Сравнение: до и после

### **Раньше:**
```
Открыли игру → Сразу игра → Нужно ввести только имя
```

### **Теперь:**
```
Открыли игру → Экран логина → Вход → Игра
```

**Преимущества:**
- ✅ Настоящая авторизация с паролем
- ✅ Защита от "чужих" входов
- ✅ Каждый игрок имеет свой аккаунт
- ✅ Профессиональная система

---

## 💾 Структура данных

### **localStorage содержит:**

1. **`chessTrainerUsers`** - все пользователи
   ```javascript
   [
     { username: "test", password: "test", createdAt: "..." },
     { username: "player1", password: "12345", createdAt: "..." }
   ]
   ```

2. **`chessTrainerSession`** - текущая сессия
   ```javascript
   { username: "test", loginTime: "..." }
   ```

3. **`chessTrainerLeaderboard`** - таблица рекордов
   ```javascript
   [ { user: "test", score: 1500, ... }, ... ]
   ```

---

## 🔐 Безопасность

### **⚠️ Важно!**

Это **учебная система** для хакатона. В реальном приложении:
- ❌ **НЕ храните пароли в открытом виде!**
- ✅ Используйте хеширование (bcrypt, SHA-256)
- ✅ Используйте backend для авторизации
- ✅ Добавьте HTTPS

### **Для хакатона:**
- ✅ Достаточно для демонстрации
- ✅ Показывает понимание авторизации
- ✅ Работает локально

---

## 🎓 Для хакатона

### **Что говорить судьям:**

> "Мы реализовали полноценную систему авторизации с логином и паролем. При запуске приложения пользователь попадает на экран входа. Есть возможность регистрации новых пользователей с валидацией данных. Сессии сохраняются в localStorage, поэтому при обновлении страницы пользователь остается залогиненным. Есть тестовый аккаунт test/test для демонстрации. Система защищена от дубликатов логинов и коротких паролей."

### **Ключевые моменты:**
- ✅ Профессиональная система входа
- ✅ Валидация данных
- ✅ Сохранение сессий
- ✅ Тестовый пользователь для демо

---

## 📋 Новые возможности

| Функция | Описание |
|---------|----------|
| Экран логина | Первый экран при открытии |
| Экран регистрации | Создание новых пользователей |
| Валидация | Проверка длины и уникальности |
| Сессии | Автоматический вход при F5 |
| Тестовый пользователь | test/test для быстрого входа |
| Выход | Возврат на экран логина |

---

## 🚀 Быстрый старт

### **Для тестирования:**
```
1. Откройте index.html
2. Логин: test
3. Пароль: test
4. Нажмите Enter или "Войти"
5. Готово! Вы в игре!
```

### **Для создания своего аккаунта:**
```
1. На экране логина кликните "Регистрация"
2. Введите свой логин (мин. 3 символа)
3. Введите свой пароль (мин. 3 символа)
4. Нажмите "Регистрация"
5. Автоматический вход в игру!
```

---

## 🔍 Проверка в консоли

### **Посмотреть всех пользователей:**
```javascript
JSON.parse(localStorage.getItem('chessTrainerUsers'))
```

### **Посмотреть текущую сессию:**
```javascript
JSON.parse(localStorage.getItem('chessTrainerSession'))
```

### **Добавить пользователя вручную:**
```javascript
let users = JSON.parse(localStorage.getItem('chessTrainerUsers') || '[]');
users.push({ username: 'admin', password: 'admin123', createdAt: new Date().toISOString() });
localStorage.setItem('chessTrainerUsers', JSON.stringify(users));
```

### **Выйти из всех аккаунтов:**
```javascript
localStorage.removeItem('chessTrainerSession');
location.reload();
```

---

## 📐 Изменённые файлы

### **1. `index.html`**
- Добавлен `<div class="auth-screen">` с формами логина и регистрации
- Игровой экран обёрнут в `<div class="game-screen">`
- Убрана форма регистрации из игры

### **2. `css/style.css`**
- Добавлены стили `.auth-screen`, `.auth-container`
- Стили для `.auth-input`, `.auth-btn`
- Стили для `.auth-switch` (ссылки)

### **3. `js/script.js`**
- Функция `initializeAuth()` - инициализация
- Функция `initializeUsers()` - создание тестового пользователя
- Функция `handleLogin()` - обработка входа
- Функция `handleRegister()` - обработка регистрации
- Функция `showGameScreen()` - показ игры
- Функция `showAuthScreen()` - показ логина
- Обновлена `logoutUser()` - возврат на логин

---

## ✅ Готово к использованию!

**Что изменилось:**

| Было | Стало |
|------|-------|
| Игра сразу при открытии | Сначала логин |
| Только имя пользователя | Логин + пароль |
| Без защиты | С проверкой пароля |
| Форма регистрации в игре | Отдельный экран |
| Нет тестового пользователя | Есть test/test |

---

## 🎉 Статус: ЗАВЕРШЕНО!

**Система авторизации полностью работает!**

- ✅ Экран логина при запуске
- ✅ Экран регистрации
- ✅ Тестовый пользователь test/test
- ✅ Сохранение сессий
- ✅ Выход с возвратом на логин
- ✅ Валидация данных
- ✅ Нет ошибок в коде

---

**Дата:** 5 ноября 2025  
**Версия:** 4.0 (система авторизации)  
**Статус:** ✅ ПОЛНОСТЬЮ ГОТОВО

