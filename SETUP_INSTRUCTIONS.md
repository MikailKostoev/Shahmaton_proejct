# 🔥 Инструкция по настройке Firebase Authentication

## ⚠️ ВАЖНО! Необходимо настроить Firebase Authentication

Ваше приложение готово к использованию, но перед запуском нужно включить аутентификацию в Firebase Console.

## 📋 Пошаговая инструкция:

### Шаг 1: Откройте Firebase Console
1. Перейдите на https://console.firebase.google.com/
2. Выберите ваш проект **shahmaton-dd992**

### Шаг 2: Включите Email/Password Authentication
1. В левом меню выберите **"Authentication"** (Аутентификация)
2. Нажмите **"Get started"** (если это первый раз)
3. Перейдите на вкладку **"Sign-in method"** (Способ входа)
4. Найдите **"Email/Password"** в списке провайдеров
5. Нажмите на него и включите (**Enable**)
6. Нажмите **"Save"** (Сохранить)

### Шаг 3: (Опционально) Настройте Firestore для таблицы рекордов
Если хотите сохранять рекорды в облаке:
1. В левом меню выберите **"Firestore Database"**
2. Нажмите **"Create database"**
3. Выберите режим **"Start in test mode"** (для разработки)
4. Выберите регион (например, europe-west)
5. Нажмите **"Enable"**

### Шаг 4: Настройте правила безопасности Firestore
Замените правила по умолчанию на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Таблица рекордов
    match /leaderboard/{entry} {
      // Все могут читать
      allow read: if true;
      // Писать могут только аутентифицированные пользователи
      allow create: if request.auth != null && 
                       request.resource.data.userId == request.auth.uid;
      // Обновлять и удалять могут только владельцы записей
      allow update, delete: if request.auth != null && 
                               resource.data.userId == request.auth.uid;
    }
  }
}
```

## 🎮 Тестовый пользователь

В приложении уже настроен **тестовый пользователь**:
- **Логин:** test
- **Пароль:** test

При первом входе этот пользователь будет автоматически создан в системе.

## ✅ Проверка

После выполнения всех шагов:
1. Откройте `index.html` в браузере
2. Нажмите кнопку **"Войти"**
3. Введите: **test** / **test**
4. Должен произойти успешный вход!

## 🚀 Что работает сейчас:

✅ **Аутентификация:**
- Регистрация новых пользователей
- Вход существующих пользователей
- Выход из системы
- Тестовый пользователь test/test
- Показ/скрытие пароля
- Автоматическое сохранение состояния

✅ **Безопасность:**
- Рекорды сохраняются только для залогиненных пользователей
- Игра доступна только после входа
- Логины не конфликтуют между пользователями

✅ **Интерфейс:**
- Красивый поп-ап логина/регистрации
- Переключение между формами
- Валидация данных
- Понятные сообщения об ошибках

## 📝 Техническая информация

### Структура аутентификации:
- **Логины** конвертируются в email формата: `username@shahmaton.local`
- Это позволяет использовать Email/Password провайдер Firebase
- Реальные email адреса не требуются

### Файлы проекта:
- `index.html` - Основной HTML с поп-апом аутентификации
- `css/style.css` - Стили для поп-апа
- `js/firebase-auth.js` - Логика аутентификации Firebase
- `js/script.js` - Игровая логика с проверкой аутентификации
- `js/chess-rules.js` - Правила шахмат

## 🔮 Следующие шаги (опционально):

1. **Сохранение рекордов в Firestore** - вместо localStorage
2. **Глобальная таблица рекордов** - рекорды всех игроков
3. **Профили пользователей** - статистика, достижения
4. **Восстановление пароля** - через email
5. **Социальные входы** - Google, Facebook и т.д.

---

💡 **Нужна помощь?** Проверьте консоль браузера (F12) на наличие ошибок.

